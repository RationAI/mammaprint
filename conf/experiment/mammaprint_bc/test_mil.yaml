# @package _global_
defaults:
  - /datamodule: wsi
  - /datamodule/data_sources: mil_luminal_test
  - /datamodule/datasets@datamodule.datasets.test: mil_dataset
  - /datamodule/datasets/sampler@datamodule.datasets.test.sampler: mil_tree
  - /datamodule/datasets/augmentations@datamodule.datasets.test.augmentations: normalize
  - /ml: mammaprint
  - /ml/net: saved_model
  - /ml/output_activation: sigmoid
  - /ml/metrics:
    - binary_accuracy
    - binary_auroc
    - binary_recall
    - binary_specificity
  - _self_

ml:
  net:
    model_uri: ???

datamodule:
  datasets:
    test:
      sampler:
        index_levels:
          - slide_name
  dataloaders_kwargs:
    test:
      num_workers: 8
      batch_size: 40

metadata:
  experiment_name: mammaprint
  run_name: VGG16 test whole slide images
  description: |
    Luminal binary classification.
  hyperparameters:
    num_workers: 8
    batch_size: 40

mlflow-artifacts:/17/3aaf24a1ed194fd4b6c3d124b476149c/artifacts/dataset