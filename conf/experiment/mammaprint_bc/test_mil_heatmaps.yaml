# @package _global_
defaults:
  - /experiment/mammaprint_bc: test_mil
  - /trainer/callbacks:
    - attention_visualizer
  - _self_

ml:
  net:
    model_uri: mlflow-artifacts:/17/1ef85707a8334f6c94c1cf9a987c55d8/artifacts/model/MLFlowModelCheckpoint/MIL_binary_best
datamodule:
  datasets:
    test:
      sampler:
        index_levels:
          - slide_name
      label: "luminal_id"
  dataloaders_kwargs:
    test:
      num_workers: 6
      batch_size: 16

trainer:
  callbacks:
    attention_visualizer:
      save_dir: test_heatmaps

metadata:
  experiment_name: mammaprint
  run_name: MIL Luminal test whole slide images with attention heatmaps, using the gated attention model
  description: |
    Luminal binary classification.
  hyperparameters:
    num_workers: 6
    batch_size: 16
   