# @package _global_
defaults:
  - /experiment/mammaprint_bc: train
  - override /ml/net: resnet152
  - _self_

ml:
  net:
    features:
      weights: DEFAULT

trainer:
  callbacks:
    log_best_model:
      filename: resnet152_mammaprint_best

metadata:
  run_name: ResNet152 training
  description: |
    Luminal binary classification.
    The goal is to train the ResNet152 model.
