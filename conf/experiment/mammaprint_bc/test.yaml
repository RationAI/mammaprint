# @package _global_
defaults:
  - /datamodule: wsi
  - /datamodule/data_sources: mammaprint_test
  - /datamodule/datasets@datamodule.datasets.test: base_classification
  - /datamodule/datasets/sampler@datamodule.datasets.test.sampler: sequential_tree
  - /datamodule/datasets/augmentations@datamodule.datasets.test.augmentations: normalize
  - /ml: mammaprint
  - /ml/net: saved_model
  - /ml/output_activation: sigmoid
  - /ml/metrics:
    - binary_accuracy
    - binary_auroc
    - binary_recall
    - binary_specificity
  - _self_

ml:
  net:
    model_uri: ???

datamodule:
  datasets:
    test:
      sampler:
        index_levels:
          - slide_name
  dataloaders_kwargs:
    test:
      num_workers: 8
      batch_size: 40

metadata:
  experiment_name: mammaprint
  run_name: VGG16 test whole slide images
  description: |
    Luminal binary classification.
  hyperparameters:
    num_workers: 8
    batch_size: 40
