# @package _global_
defaults:
  - /experiment/mammaprint_bc: train
  - override /ml/net: resnet101
  - _self_

ml:
  net:
    features:
      weights: DEFAULT

trainer:
  callbacks:
    log_best_model:
      filename: resnet101_mammaprint_best

metadata:
  run_name: ResNet101 training after segmentation
  description: |
    Luminal binary classification.
    The goal is to train the ResNet101 model.
