"""Post-hoc explanations for MIL models over stored tile embeddings.

This package is deliberately separate from :mod:`ml.models`: it loads a trained
model in evaluation mode and never changes the training or forward contracts.
"""
