[project]
name = ""
version = "0.1.0"
description = ""
authors = []
requires-python = ">=3.11,<3.12"
readme = "README.md"
license = { file = "LICENSE" }


#####################################
# Dependencies
#####################################

dependencies = [
    "numpy>=1.26.2",
    "polars>=0.20.2",
    "pandas>=2.1.4",
    "scikit-learn>=1.3.2",
    "scipy>=1.11.4",
    "pyvips>=2.2.1",
    "torch>=2.1.2",
    "torchvision>=0.16.2",
    "torchmetrics>=1.2.1",
    "Pillow>=10.1.0",
    "tqdm>=4.66.1",
    "pytest>=7.4.3",
    "albumentations>=1.3.1",
    "lightning>=2.1.3",
    "omegaconf>=2.3.0",
    "hydra-core>=1.3.2",
    "mlflow>=2.9.2",
    "captum>=0.7.0",
    "gitpython>=3.1.40",
    "humanize>=4.9.0",
    "scikit-image>=0.22.0",
    "SlideLip @ git+ssh://git@gitlab.ics.muni.cz/rationai/digital-pathology/pipeline/slidelip",
    "report @ git+ssh://git@gitlab.ics.muni.cz/rationai/digital-pathology/pipeline/report.git",
    "kneed>=0.8.5",
    "nptyping>=2.5.0",
]

[tool.pdm.dev-dependencies]
dev = ["mypy", "pre-commit", "ruff"]
test = ["pytest", "pytest-cov"]


#####################################
# Scripts
#####################################

[tool.pdm.scripts]
train = "python -m histopipe.fit +experiment={args}"
eval = "python -m histopipe.test +experiment={args}"

l = { composite = ["lint", "format", "mypy"] }
test = "pytest tests"
lint = "ruff check ."
format = "ruff format ."
mypy = "mypy ."
post_install = { composite = [
    "pre-commit install",
    "pre-commit install --hook-type commit-msg",
] }


#####################################
# Tool configuration
#####################################

[tool.coverage.run]
branch = true
omit = ["tests/*"]
source = ["."]

[tool.coverage.report]
exclude_also = [
    # Don't complain about abstract methods, they aren't run:
    "@(abc\\.)?abstractmethod",
]

[tool.mypy]
strict = true
ignore_missing_imports = true

[tool.ruff]
fix = true
line-length = 88
target-version = "py311"
extend-ignore = [
    "ERA001", # commented out code
    "D100",   # missing docstring in public module
    "D101",   # missing docstring in public class
    "D102",   # missing docstring in public method
    "D103",   # missing docstring in public function
    "D104",   # missing docstring in public package
    "D105",   # missing docstring in magic method
    "D106",   # missing docstring in public nested class
    "D107",   # missing docstring in __init__
]

[tool.ruff.format]
# Enable reformatting of code snippets in docstrings.
docstring-code-format = true

[tool.ruff.lint]
unfixable = [
    "ERA", # do not autoremove commented out code
]
extend-select = [
    "B",   # flake8-bugbear
    "C4",  # flake8-comprehensions
    "ERA", # flake8-eradicate/eradicate
    "I",   # isort
    "N",   # pep8-naming
    "PIE", # flake8-pie
    "PGH", # pygrep
    "RUF", # ruff checks
    "SIM", # flake8-simplify
    "TCH", # flake8-type-checking
    "TID", # flake8-tidy-imports
    "UP",  # pyupgrade
    "D",   # pydocstyle 
]

[tool.ruff.lint.flake8-tidy-imports]
ban-relative-imports = "all"

[tool.ruff.lint.isort]
lines-after-imports = 2
known-first-party = ["tests"]

[tool.ruff.lint.pydocstyle]
convention = "google"

[tool.pytest.ini_options]
filterwarnings = ["ignore::DeprecationWarning"]
